# Отчет по лабораторной работе №2

## Титульные данные

- ФИО: `________________`
- Группа: `________________`
- Вариант: `1`

## Условие задачи

Реализовать программное средство для RSA:
- вычисление открытого ключа `Ko` по введенным `p`, `q`, `Kc`;
- побайтовое шифрование произвольного файла;
- расшифрование файла, где каждый 16-битный блок — это зашифрованный 8-битный байт;
- проверки ограничений ввода;
- вывод содержимого зашифрованного файла на экран в виде десятичных чисел.

## Использованные алгоритмы

### 1) Быстрое возведение в степень

Пример (уникальные числа):
- `11^23 mod 53 = ____`

Вставить таблицу шагов из программы (пункт меню `3`).

### 2) Поиск первообразных корней

Число для поиска: `p = 43`.

Вставить:
- простые делители `p-1`;
- список всех первообразных корней по модулю `43`.

### 3) Расширенный алгоритм Евклида

Пример (взаимно простые числа):
- `a = 899`, `b = 493`.

Вставить таблицу итераций и проверку:
- `x*a + y*b = gcd(a,b)`.

## Описание программы

- Язык: Java;
- Основные файлы: `Main.java`, `RSAService.java`, `MathUtils.java`, `PrimitiveRootFinder.java`, `AlgorithmShowcase.java`;
- Формат шифро-файла: поток 16-битных блоков (по 2 байта на каждый исходный байт).

## Тестирование

### Тест 1: корректное шифрование/дешифрование

- Входные параметры: `p=____`, `q=____`, `Kc=____`;
- Файл: `________________`;
- Результат: исходный и расшифрованный файлы совпадают.

### Тест 2: проверка ошибок ввода

Показать скриншоты для случаев:
- `p` или `q` не простое;
- `p=q`;
- `p*q <= 255` или `p*q > 65535`;
- `gcd(Kc, phi(r)) != 1`.

## Скриншоты

- Скрин 1: шифрование и вывод десятичных блоков;
- Скрин 2: расшифрование;
- Скрин 3: работа трех алгоритмов (пункт меню `3`).

## Вывод

Кратко описать, что программа реализует требования варианта и корректно обрабатывает данные/ошибки.
